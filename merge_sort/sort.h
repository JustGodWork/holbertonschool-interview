#ifndef SORT_H
#define SORT_H

#include <stdlib.h>

/* Prototype for provided helper that prints arrays */
void print_array(const int *array, size_t size);

/* Prototype for merge sort implementation */
void merge_sort(int *array, size_t size);

#endif /* SORT_H */
